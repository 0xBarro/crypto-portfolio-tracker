const processEthWallet = (wallet: string, blockchainName: string, debug=false): object => {
    console.log(`Now processing wallet ${wallet} on blockchain ${blockchainName}`)
    return {}
}

export default processEthWallet