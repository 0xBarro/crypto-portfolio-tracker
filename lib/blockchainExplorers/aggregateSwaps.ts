import { processedSwap, processedTx } from "./interfaces"

// Aggregates all the swaps for each wallet, and calculated capital gains
const aggregateSwaps = (txList: processedTx[], wallet: string, chain: string) => {
    1
}

export default aggregateSwaps